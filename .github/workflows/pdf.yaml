# .github/workflows/pdf.yml
name: Build PDFs

on:
  push:
    branches: [ main ]
    paths:
      - 'pdf/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-pdfs:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Build all PDFs with Marp (directory → pdf/output/)
        uses: docker://marpteam/marp-cli:v4.2.3@sha256:472c9e9203391568cd777a70455ee241ee8d16a123dbbc2329277a35b6c56fc8
        with:
          args: >-
            -I pdf/
            --pdf
            --allow-local-files
            --title "T81 Foundation Specification"
            --output pdf/output/
        env:
          # Fix permission issues between container and GitHub runner
          MARP_USER: root:root

      - name: Upload PDFs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: t81-spec-pdfs
          path: pdf/output/*.pdf
          if-no-files-found: error
