# tools/vscode-t81/syntaxes

TextMate grammar definitions for T81 syntax highlighting.

## Primary file
- `t81.tmLanguage.json`

## Notes
- Grammar changes should reflect actual lexer/parser token behavior.
- Validate against representative files in `examples/` and language fixtures in `tests/`.
