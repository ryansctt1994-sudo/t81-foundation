# Глава 1: Введение

## 1.1 Область применения и определение

**Статус: Реализовано и протестировано**

Проект **T81 Foundation** реализует архитектуру детерминированной виртуальной машины на базе троичной логики, разработанную для верифицируемых вычислений. В отличие от сред исполнения общего назначения, которые ставят в приоритет пропускную способность или аппаратную абстракцию, T81 ставит во главу угла **побитовую воспроизводимость** и **аудируемость**.

Система определяется следующими ключевыми инвариантами:
1.  **Строгий детерминизм**: Выполнение валидной программы TISC (Ternary Instruction Set Computer) $P$ на входных данных $I$ производит последовательность переходов состояний $S_0 \to S_1 \to \dots \to S_n$, которая идентична на всех совместимых хост-архитектурах (x86_64, ARM64).
2.  **Нативная троичность**: Архитектура работает на сбалансированной троичной логике (триты $\in \{-1, 0, 1\}$), используя собственный арифметический стек (`dmath`) для избежания недетерминизма двоичных чисел с плавающей запятой.
3.  **Принудительное соблюдение политик**: Все выполнение управляется **Ядром Axion**, супервизором на основе возможностей (capability-based), который обеспечивает соблюдение политик безопасности (лимиты рекурсии, границы памяти, этические ограничения) перед завершением инструкции.

> **Якорь верификации**: Цикл детерминированного выполнения реализован в `src/vm/vm.cpp` (см. `Interpreter::step()`). Примитивы троичной арифметики определены в `include/t81/ternary.hpp` и `include/t81/core/T81Float.hpp`.

## 1.2 Архитектура системы

Стек T81 состоит из четырех основных уровней, каждый из которых имеет четкие обязанности и границы верификации.

### 1.2.1 Виртуальная машина TISC (T81VM)

**Статус: Реализовано и протестировано**

T81VM — это стековый интерпретатор для набора инструкций **Ternary Instruction Set Computer (TISC)**. Он управляет сегментированной моделью памяти, включающей:
*   **Код**: Сегмент инструкций только для чтения.
*   **Стек**: Хранилище LIFO для локальных переменных и адресов возврата.
*   **Куча**: Динамическое выделение памяти для сложных объектов (Тензоры, Графы).
*   **Тензор**: Специализированное хранилище для многомерных числовых данных.
*   **Мета**: Возможности рефлексии и интроспекции.

Состояние VM формально определяется как кортеж $S = (R, PC, SP, M_{seg}, \Phi)$, где $R$ представляет регистровый файл (81 регистр), $PC$ — счетчик команд, $SP$ — указатель стека, $M_{seg}$ — сегменты памяти, и $\Phi$ — флаги статуса.

> **Ссылка**: См. `src/vm/vm.cpp`, структура `State`.

### 1.2.2 Ядро безопасности Axion

**Статус: Реализовано и протестировано**

Axion действует как гипервизор для T81VM. Он перехватывает каждую отправку инструкции для проверки соответствия активной **Политике**. Политики — это декларативные наборы правил, которые ограничивают:
*   **Использование ресурсов**: Лимиты выделения памяти, количество циклов.
*   **Поток управления**: Глубина рекурсии, сложность ветвления.
*   **Возможности**: Доступ к вводу-выводу, сети или системным вызовам файловой системы.

Если инструкция нарушает политику, Axion выдает вердикт `Deny`, вызывая прерывание VM с ошибкой `SecurityFault`.

> **Ссылка**: Логика политик реализована в `src/axion/policy_engine.cpp` и `include/t81/axion/api.hpp`.

### 1.2.3 Каноническая файловая система (CanonFS)

**Статус: Частичная реализация**

CanonFS — это слой хранения с адресацией по содержимому, который гарантирует **структурную неизменность**. Объекты (веса, код, данные) идентифицируются по их хэшу SHA3-256 (`CanonHash81`). Загрузка объекта из CanonFS гарантирует, что данные в памяти побитово идентичны артефакту, который был подписан и опубликован, исключая атаки через "дрейф зависимостей".

> **Ссылка**: Реализовано в `src/canonfs/` и определено в `spec/canonfs-spec.md`. В настоящее время поддерживает базовую проверку хэшей и загрузку.

### 1.2.4 Когнитивные уровни

**Статус: Реализовано (Уровни 1-5)**

T81 организует вычислительную сложность в **Когнитивные Уровни**, от чистой арифметики (Уровень 1) до бесконечных рекурсивных форм (Уровень 5).
*   **Уровень 1 (Символьный)**: Базовая арифметика и логика.
*   **Уровень 2 (Рефлексивный)**: Самоинспекция и захват трассы.
*   **Уровень 3 (Рекурсивный)**: Ограниченная рекурсия и генерация доказательств.
*   **Уровень 4 (Распределенный)**: Протоколы сплетен (gossip) и слияние состояний.
*   **Уровень 5 (Бесконечный)**: Геометрические ряды и нетерминируемые формы.

> **Ссылка**: Логика уровней находится в `src/cog/`. См. `src/cog/tier3/recursive.cpp` и `src/cog/tier5/infinite.cpp`.

## 1.3 Миссия верифицируемых вычислений

Основное применение T81 — это **Суверенные Вычисления**: возможность выполнять код и верифицировать результат, не доверяя оператору оборудования. Объединяя строгую программно-определяемую арифметику (`dmath`) с криптографическим журналом аудита (Axion Trace), T81 обеспечивает:
*   **Инференс ИИ без доверия**: Верификацию того, что конкретная модель произвела конкретный результат.
*   **Смарт-контракты**: Выполнение логики, где консенсус зависит от побитово точных переходов состояний.
*   **Научную воспроизводимость**: Гарантию того, что симуляции, запущенные в 2025 году, дадут те же результаты в 2050 году.

## 1.4 Терминология

| Термин | Определение |
| :--- | :--- |
| **Трит** | Цифра по основанию 3: $\{-1, 0, 1\}$. |
| **Трайт** | Последовательность тритов, обычно 3 или 9. |
| **TISC** | Ternary Instruction Set Computer (ISA - архитектура набора команд). |
| **Axion** | Ядро безопасности и принудительного соблюдения политик. |
| **CanonRef** | Каноническая ссылка (хэш) на неизменяемый объект. |
| **promotion** (продвижение) | Акт повышения привилегий или возможностей уровня. |

## 1.5 Чек-лист верификации

*   [ ] **детерминизм**: Производит ли VM идентичные трассы на x86 и ARM? (Проверяется `scripts/ci/t81lang_repro_gate.py`)
*   [ ] **изоляция**: Корректно ли Axion перехватывает запрещенные инструкции? (Проверяется `tests/cpp/test_ethics.cpp`)
*   [ ] **постоянство**: Корректно ли CanonFS извлекает объекты по хэшу? (Проверяется `tests/cpp/canonfs_driver_test.cpp`)
