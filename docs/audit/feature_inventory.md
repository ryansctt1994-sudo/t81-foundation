# Feature Inventory Matrix

| ID | Feature Name | PDF Claim (Legacy) | Spec Requirement (t81lang-spec.md) | Implementation Status (Repo) | Evidence | Notes |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **L01** | **Primitive Types** | Inferred: `T81Int`, `T81Float`, etc. | Section 2.2: `T81Int`, `T81Float`, `T81Fraction`, `Symbol` required. | **IMPLEMENTED** | `src/frontend/parser.cpp` (keywords), `include/t81/core/T81*.hpp` | Fully supported. |
| **L02** | **Vector Literals** | Inferred: `[1, 2, 3]` syntax. | Section 1: `[expr, ...]` and `[expr; count]`. | **IMPLEMENTED** | `src/frontend/parser.cpp` (parsing), `include/t81/frontend/ir_generator.hpp` (lowering) | `[expr; count]` syntax parsed; semantic analysis handles expansion. |
| **L03** | **Structural Types** | Inferred: `Option`, `Result`. | Section 2.5: `Option[T]`, `Result[T, E]`. | **IMPLEMENTED** | `src/frontend/parser.cpp` (generic syntax), `include/t81/frontend/ir_generator.hpp` (constructors/lowering) | Lowers to specific TISC opcodes `MAKE_OPTION_*`, `MAKE_RESULT_*`. |
| **L04** | **Legacy Generics `<T>`** | Inferred: Angle brackets used. | Section 2.1: Explicitly FORBIDDEN. | **REMOVED** | `src/frontend/parser.cpp` (explicit error check) | Parser enforces modern `[...]` syntax. |
| **L05** | **Recurse Statement** | Inferred: Explicit recursion control. | Section 6.4: Recursion allowed with limits. | **PARTIAL** | `src/frontend/parser.cpp` (parsed), `include/t81/frontend/ir_generator.hpp` (transparent visit) | Parsed but lowers to a simple block; no special VM opcode emitted. Recursion depth checked by VM `CALL`. |
| **L06** | **Distributed Statement** | Inferred: Distributed execution block. | Section 0.6: Tier-aware code. | **PARTIAL** | `src/frontend/parser.cpp` (parsed), `include/t81/frontend/ir_generator.hpp` (transparent visit) | Parsed but lowers to a simple block; semantics not enforced by IR. |
| **L07** | **Infinite Statement** | Inferred: Infinite computation block. | Section 6.3: `@bounded(infinite)` loops. | **PARTIAL** | `src/frontend/parser.cpp` (parsed `infinite {}` and `@bounded(infinite)`), `include/t81/frontend/ir_generator.hpp` (loop annotation handled) | `InfiniteStmt` block is transparent. `@bounded(infinite)` loop annotation works (unconditional jump). |
| **L08** | **Stdlib Math** | Inferred: `sin`, `cos`, `log`, etc. | Section 0.1: Deterministic math. | **IMPLEMENTED** | `include/t81/frontend/ir_generator.hpp` (hardcoded calls) | Maps `std.math.*` calls directly to TISC opcodes (`FSIN`, `FCOS`, etc.). |
| **L09** | **Stdlib Collections** | Inferred: `Map`, `Set`, `Graph`. | Section 2.4: Composite types. | **IMPLEMENTED** | `include/t81/frontend/ir_generator.hpp` (hardcoded calls) | Maps `std.collections.*` to TISC opcodes (`VECLEN`, `VECPUSH`, `STRVECNEW`, etc.). |
| **L10** | **Record/Enum Defs** | Inferred: Custom types. | Section 2.4: `record`, `enum`. | **IMPLEMENTED** | `src/frontend/parser.cpp`, `include/t81/frontend/ir_generator.hpp` | Emits `TypeAliasMetadata` in IR. |
| **L11** | **Match Expressions** | Inferred: Pattern matching. | Section 6.2: `match` on structural types. | **IMPLEMENTED** | `src/frontend/parser.cpp`, `include/t81/frontend/ir_generator.hpp` | Complex lowering logic for `Option`, `Result`, `Enum` patterns. |
| **L12** | **Control Flow** | Inferred: `if`, `loop`. | Section 6.1, 6.3: `if`, `loop`, `match`. | **IMPLEMENTED** | `src/frontend/parser.cpp`, `include/t81/frontend/ir_generator.hpp` | Lowers to `JMP`, `JZ`, `JNZ` sequences. |
| **L13** | **Determinism** | Inferred: Reproducible builds. | Section 5: "Eight deterministic stages". | **IMPLEMENTED** | `tests/fixtures/t81lang_determinism/` | CI gates enforce bit-exact output for AST and IR. |
