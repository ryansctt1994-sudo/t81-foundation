fn main() -> i32 {
  let a: Tensor = std.tensor.from_list([1, 2, 3]);
  let b: Tensor = std.tensor.from_list([4, 5, 6]);
  let c: Tensor = std.tensor.vec_add(a, b);
  let _ = c;
  let handle: i32 = std.tensor.load("encoder.weight");
  print(handle);
  return 0;
}
